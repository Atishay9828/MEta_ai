#include <iostream>
#include "../env/NegotiationEnv.h"

void run_simulation() {
    NegotiationEnv env;
    env.reset();
    
    // Placeholder for simulation loop
    std::cout << "Simulation initialized." << std::endl;
}

int main() {
    run_simulation();
    return 0;
}
